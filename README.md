# website-security
Wordpress Website Security Points

# Must-Use Plugin Folder & .htaccess Rules
Step 1: If the folder does not already exist, create a folder named mu-plugins inside the /wp-content/ directory.
Step 2: Upload the block-bad-http-hosts.php file to the mu-plugins folder.
